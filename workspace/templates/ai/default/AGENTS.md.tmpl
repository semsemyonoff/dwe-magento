# Service hub: {{ .Service }}

This directory is a **dwe service workspace** for the `{{ .Service }}` service of project `{{ .Project.Name }}`. It is a generated hub — not the application repository itself.

The application code lives in `src/`. If the project ships its own `AGENTS.md` or `CLAUDE.md` inside `src/`, treat that as authoritative for code-level conventions (framework, structure, tests, lint). It may also be absent — in that case orient yourself from whatever the project provides (`README.md`, the package manifest, the test layout, etc.).

## Workspace layout

- `src/` — the application repository (project source; may have its own `AGENTS.md` / `CLAUDE.md`)
- `configs/` — service config files mounted into the container
- `home/` — container user `$HOME` (shell history, tool caches)
- `runtime/` — ephemeral runtime state (sockets, pids, scratch)
- `.devcontainer/` — Dev Containers config (generated)
- `.vscode/` — VS Code workspace settings (generated)
- `AGENTS.md` / `CLAUDE.md` — this file (generated; `CLAUDE.md` is a symlink to `AGENTS.md`)

Everything outside `src/` is owned by dwe: regenerated on `dwe render` / deploy, gitignored at the project root, and safe to recreate. Do not edit this file by hand — it is rendered from `workspace/templates/ai/`.

## Container

- Container: `{{ .ServiceCfg.Container }}`
- Work dir (inside container): `{{ .ServiceCfg.WorkDirInternal }}`
{{- if .ServiceCfg.CLI.User }}
- User (inside container): `{{ .ServiceCfg.CLI.User }}`
{{- end }}
{{- if .ServiceCfg.DependsOn }}
- Depends on: {{ range $i, $d := .ServiceCfg.DependsOn }}{{ if $i }}, {{ end }}`{{ $d }}`{{ end }}
{{- end }}

The service can be operated through the parent dwe project, but is also a standalone application — running it through dwe is not required.
