commands:
  create:
    type: workflow
    private: true
    description: Create the Magento service database
    steps:
      - command: db.create
        with:
          database: "${vars.db.database}"
